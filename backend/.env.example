# backend 환경변수 — 복사해서 .env로 사용 (backend/.env는 gitignore)
# 새 변수는 app/core/config.py Settings 필드와 함께 추가한다.

DATABASE_URL=sqlite+aiosqlite:///./dev.db
ANTHROPIC_API_KEY=

# 관측 (선택 — 켜면 LangSmith 추적)
# LANGSMITH_TRACING=true
# LANGSMITH_API_KEY=
# LANGSMITH_PROJECT=halfday-backend
